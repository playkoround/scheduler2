package com.sparta.scheduler.repository;

import com.sparta.scheduler.entity.Reply;
import org.springframework.data.jpa.repository.JpaRepository;

public class ReplyRepository implements JpaRepository<Reply, long> {
}
